
def min(a: Int, b: Int): Int {
	if (a < b) {
		return a
	} else {
		return b
	}
}

def max(a: Int, b: Int): Int {
	if (a > b) {
		return a
	} else {
		return b
	}
}

def diff(a: Int, b: Int): Int {
	var mn = min(a, b)
	var mx = max(a, b)
	return mx - mn
}
